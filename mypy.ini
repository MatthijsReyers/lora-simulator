[mypy]
python_version = 3.12
explicit_package_bases = true
ignore_missing_imports = true
implicit_optional = true

# Per-module overrides for files with pre-existing type errors.
# These should be removed as the errors are fixed.

[mypy-tests]
ignore_errors = true
